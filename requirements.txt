# Зависимости Python. Установка:  pip install -r requirements.txt

requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0

pymorphy3>=2.0

wordcloud>=1.9
matplotlib>=3.8

# rpy2, основной способ связи с R. Ставится отдельно, т.к. на Windows бывает капризным
# без него программа сама вызовет R через Rscript:
# rpy2>=3.6
